'use client';
import { useState } from 'react'; import Link from 'next/link'; import { Button } from '@/components/ui/button'; import { Input } from '@/components/ui/input'; import { useAuth } from '@/contexts/providers';
export default function Forgot(){ const {reset}=useAuth(); const [email,setEmail]=useState(''); const [msg,setMsg]=useState(''); return <main className="grid min-h-screen place-items-center bg-slate-50 p-6 dark:bg-navy-900"><form className="glass w-full max-w-md rounded-3xl p-8" onSubmit={async e=>{e.preventDefault(); await reset(email); setMsg('Password reset email sent if the account exists.')}}><h1 className="text-3xl font-black">Reset password</h1><p className="mt-2 text-sm text-slate-500">Only existing employee accounts can reset passwords.</p><Input className="mt-6" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="employee@Axienta.com" required/><Button className="mt-4 w-full">Send reset link</Button>{msg&&<p className="mt-4 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{msg}</p>}<Link className="mt-4 block text-center text-sm text-gold-600" href="/login">Back to login</Link></form></main> }
