'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { motion } from 'framer-motion'; import { Button } from '@/components/ui/button'; import { Input, Select } from '@/components/ui/input'; import { useAuth } from '@/contexts/providers'; import { roles } from '@/lib/roles'; import { Role } from '@/types';
export default function Login(){ const {login,demoLogin}=useAuth(); const router=useRouter(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [role,setRole]=useState<Role>('CEO'); const [err,setErr]=useState(''); const submit=async(e:React.FormEvent)=>{e.preventDefault(); setErr(''); try{ await login(email,password); router.push('/dashboard'); }catch(ex:any){ setErr(ex.message||'Login failed'); }}; return <main className="grid min-h-screen bg-navy-900 lg:grid-cols-2"><section className="hidden p-10 text-white lg:flex lg:flex-col lg:justify-between"><div className="flex items-center gap-3"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-gold-500 text-xl font-black text-navy-900">A</div><div><h1 className="text-2xl font-black">Axienta Business Consulting</h1><p className="text-slate-300">ERP • CRM • Employee Management</p></div></div><motion.div initial={{opacity:0,y:30}} animate={{opacity:1,y:0}}><p className="text-sm uppercase tracking-[.3em] text-gold-400">Premium SaaS Dashboard</p><h2 className="mt-4 max-w-xl text-5xl font-black leading-tight">Secure role-based operating system for your entire company.</h2><p className="mt-5 max-w-xl text-slate-300">CEO analytics, CRM pipeline, calling, scraper, operations, HR, attendance and reports in one scalable Firebase-powered platform.</p></motion.div><p className="text-sm text-slate-400">Employees cannot self-register. Accounts are created only by CEO/Admin/Head Manager.</p></section><section className="grid place-items-center p-6"><form onSubmit={submit} className="glass w-full max-w-md rounded-3xl p-8"><h2 className="text-3xl font-black text-navy-900 dark:text-white">Welcome back</h2><p className="mt-2 text-sm text-slate-500">Login with employee ID email and admin-generated password.</p><div className="mt-6 space-y-4"><Input type="email" placeholder="employee@Axienta.com" value={email} onChange={e=>setEmail(e.target.value)} required/><Input type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/>{err&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-600">{err}</p>}<Button className="w-full" type="submit">Secure Login</Button></div><div className="my-5 flex items-center gap-3"><span className="h-px flex-1 bg-slate-200"/><span className="text-xs text-slate-400">Preview demo</span><span className="h-px flex-1 bg-slate-200"/></div><Select value={role} onChange={e=>setRole(e.target.value as Role)}>{roles.map(r=><option key={r}>{r}</option>)}</Select><Button type="button" variant="outline" className="mt-3 w-full" onClick={()=>{demoLogin(role); router.push('/dashboard')}}>Enter Demo as {role}</Button><a className="mt-4 block text-center text-sm font-semibold text-gold-600" href="/forgot-password">Forgot password?</a></form></section></main> }
