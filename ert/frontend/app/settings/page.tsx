'use client';
import { useState } from 'react'; import { AppShell } from '@/components/layout/app-shell'; import { PageHeader } from '@/components/dashboard/dashboard-components'; import { Card } from '@/components/ui/card'; import { Button } from '@/components/ui/button'; import { Input, Select } from '@/components/ui/input'; import { useAuth } from '@/contexts/providers';
export default function Settings(){ const {profile,updateProfileData}=useAuth(); const [name,setName]=useState(profile?.name||''); const [phone,setPhone]=useState(profile?.phone||''); return <AppShell><PageHeader title="Settings" subtitle="Profile editing, admin settings, role permission manager, audit logs and integrations."/><div className="grid gap-4 xl:grid-cols-2"><Card><h3 className="font-bold">Edit My Profile</h3><div className="mt-4 space-y-3"><Input value={name} onChange={e=>setName(e.target.value)} placeholder="Full name"/><Input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="Phone"/><Input value={profile?.email||''} disabled/><Button onClick={()=>updateProfileData({name,phone})}>Save Profile</Button></div></Card><Card><h3 className="font-bold">Integrations</h3><div className="mt-4 space-y-3"><Input placeholder="Google Maps API Key"/><Input placeholder="WhatsApp API Token"/><Input placeholder="Cloudinary Cloud Name"/><Select><option>Session Timeout: 8 hours</option><option>Session Timeout: 4 hours</option></Select><Button variant="outline">Save Integration Settings</Button></div></Card></div></AppShell> }
