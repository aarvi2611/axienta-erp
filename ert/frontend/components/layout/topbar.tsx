'use client';
import { Menu, Moon, Search, Sun, LogOut } from 'lucide-react'; import { useState } from 'react'; import { Button } from '@/components/ui/button'; import { Input } from '@/components/ui/input'; import { useAuth } from '@/contexts/providers'; import { initials } from '@/lib/utils';
export function Topbar({onMenu}:{onMenu:()=>void}){ const {profile,logout}=useAuth(); const [dark,setDark]=useState(false); const toggle=()=>{ setDark(!dark); document.documentElement.classList.toggle('dark'); }; return <header className="sticky top-0 z-30 flex h-16 items-center gap-3 border-b border-slate-200 bg-white/85 px-4 backdrop-blur dark:border-white/10 dark:bg-navy-900/80"><Button variant="ghost" className="lg:hidden" onClick={onMenu}><Menu size={20}/></Button><div className="relative hidden flex-1 md:block"><Search className="absolute left-3 top-2.5 text-slate-400" size={18}/><Input className="pl-10" placeholder="Global search leads, employees, reports..."/></div><Button variant="ghost" onClick={toggle}>{dark?<Sun size={18}/>:<Moon size={18}/>}</Button><div className="hidden text-right sm:block"><p className="text-sm font-bold">{profile?.name}</p><p className="text-xs text-slate-500 dark:text-slate-400">{profile?.role}</p></div><div className="grid h-10 w-10 place-items-center rounded-full bg-navy-800 text-sm font-bold text-white dark:bg-gold-500 dark:text-navy-900">{initials(profile?.name)}</div><Button variant="ghost" onClick={logout} title="Logout"><LogOut size={18}/></Button></header> }
