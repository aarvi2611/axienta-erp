'use client';
import { Menu, Moon, Search, Sun, LogOut } from 'lucide-react'; import { useState } from 'react'; import { Button } from '@/components/ui/button'; import { Input } from '@/components/ui/input'; import { useAuth } from '@/contexts/providers'; import { initials } from '@/lib/utils';
export function Topbar({onMenu}:{onMenu:()=>void}){ const {profile,logout}=useAuth(); const [dark,setDark]=useState(false); const toggle=()=>{ setDark(!dark); document.documentElement.classList.toggle('dark'); }; return <header className="sticky top-0 z-30 flex h-20 items-center gap-3 border-b border-slate-200/80 bg-white/90 px-4 shadow-sm backdrop-blur-xl dark:border-white/10 dark:bg-navy-900/82 md:px-6"><Button variant="ghost" className="lg:hidden" onClick={onMenu}><Menu size={20}/></Button><div className="hidden items-center gap-3 lg:flex"><div className="grid h-11 w-14 place-items-center overflow-hidden rounded-2xl bg-white p-1.5 shadow-sm ring-1 ring-slate-200"><img src="/axienta-logo.png" alt="Axienta logo" className="h-full w-full object-contain"/></div><div><p className="text-sm font-black text-navy-900 dark:text-white">Axienta ERP</p><p className="text-xs font-semibold text-orange-600">Professional Workspace</p></div></div><div className="relative hidden flex-1 md:block"><Search className="absolute left-4 top-3 text-slate-400" size={18}/><Input className="h-11 rounded-2xl bg-slate-50 pl-11 shadow-inner shadow-slate-200/40" placeholder="Search leads, employees, tasks, reports..."/></div><Button variant="ghost" className="h-11 w-11 rounded-2xl px-0" onClick={toggle}>{dark?<Sun size={18}/>:<Moon size={18}/>}</Button><div className="hidden text-right sm:block"><p className="text-sm font-black text-navy-900 dark:text-white">{profile?.name}</p><p className="text-xs font-semibold text-slate-500 dark:text-slate-400">{profile?.role}</p></div><div className="grid h-11 w-11 place-items-center rounded-2xl bg-gradient-to-br from-navy-800 to-orange-500 text-sm font-black text-white shadow-lg shadow-navy-900/15">{initials(profile?.name)}</div><Button variant="ghost" className="h-11 w-11 rounded-2xl px-0" onClick={logout} title="Logout"><LogOut size={18}/></Button></header> }
